<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Diet Tracker - CLD2</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100">
    <div class="w-full bg-white py-2 shadow-md">
      <div class="max-w-7xl mx-auto flex justify-between sm:px-6 lg:px-8">
        <div class="flex items-center">
          <div class="flex">
            <h1>Diet Tracker - CLD2</h1>
          </div>
          <% if session[:user_id] %>
          <div class="flex sm:ms-10">
            <div class="flex">
              <a href="/">Accueil</a>
            </div>
          </div>
          <% end %>
        </div>

        <% if session[:user_id] %>
        <div class="flex items-center">
          <div class="flex ml-4">
            <a href="/logout">Logout</a>
          </div>
        </div>
        <% end %>
      </div>
    </div>

    <div class="content py-6 max-w-7xl mx-auto sm:px-6 lg:px-8">
      <%= yield %>
    </div>
  </body>
</html>
